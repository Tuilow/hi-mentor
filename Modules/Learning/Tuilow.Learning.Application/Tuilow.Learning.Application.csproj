<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Tuilow.Learning.Application</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="MediatR" Version="12.4.1" />
    <PackageReference Include="FluentValidation" Version="11.11.0" />
    <PackageReference Include="FluentValidation.DependencyInjectionExtensions" Version="11.11.0" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="9.0.4" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Tuilow.Learning.Domain\Tuilow.Learning.Domain.csproj" />
    <!-- Acoplamento legítimo: matrícula depende do agregado Course (Catalog) — mesmo padrão do código original. -->
    <ProjectReference Include="..\..\Catalog\Tuilow.Catalog.Domain\Tuilow.Catalog.Domain.csproj" />
    <ProjectReference Include="..\..\..\SharedKernel\Tuilow.SharedKernel.Application\Tuilow.SharedKernel.Application.csproj" />
    <!--
      Acoplamento legítimo (mesmo padrão de Finance -> Sales): Learning reage a eventos de
      domínio publicados pelo módulo Sales (compra confirmada / pagamento de assinatura
      confirmado) para liberar o acesso ao curso automaticamente.
    -->
    <ProjectReference Include="..\..\Sales\Tuilow.Sales.Domain\Tuilow.Sales.Domain.csproj" />
    <!--
      Acoplamento legítimo (mesmo padrão acima): ao liberar acesso após compra/assinatura
      confirmada, os EventHandlers consultam o Canal do criador (Modules/Channel) para levar o
      comprador direto para /canal/{handle} em vez de só para o curso individual.
    -->
    <ProjectReference Include="..\..\Channel\Tuilow.Channel.Domain\Tuilow.Channel.Domain.csproj" />
  </ItemGroup>
</Project>
