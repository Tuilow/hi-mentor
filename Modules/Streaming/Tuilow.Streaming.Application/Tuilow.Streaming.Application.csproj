<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Tuilow.Streaming.Application</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="MediatR" Version="12.4.1" />
    <PackageReference Include="FluentValidation" Version="11.11.0" />
    <PackageReference Include="FluentValidation.DependencyInjectionExtensions" Version="11.11.0" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Tuilow.Streaming.Domain\Tuilow.Streaming.Domain.csproj" />
    <!-- Acoplamento legítimo: vincular vídeo a aula e liberar playback dependem de Course
         (Catalog). A checagem de acesso pago/matrícula (antes duplicada aqui, consultando Sales
         e Learning diretamente) agora é feita por IUserCourseAccessService (SharedKernel.Application,
         já referenciado abaixo) — por isso as referências a Sales.Domain/Learning.Domain que só
         existiam para essa checagem foram removidas. -->
    <ProjectReference Include="..\..\Catalog\Tuilow.Catalog.Domain\Tuilow.Catalog.Domain.csproj" />
    <ProjectReference Include="..\..\..\SharedKernel\Tuilow.SharedKernel.Application\Tuilow.SharedKernel.Application.csproj" />
    <!-- Acoplamento legítimo (achado M11 da auditoria): UserDeletedEventHandler reage a
         UserDeletedDomainEvent (levantado por User.MarkDeleted) para apagar os vídeos do criador
         excluído — só precisa do tipo do evento, que vive no Domain de IdentidadeAcesso. -->
    <ProjectReference Include="..\..\IdentidadeAcesso\Tuilow.IdentidadeAcesso.Domain\Tuilow.IdentidadeAcesso.Domain.csproj" />
  </ItemGroup>
</Project>
